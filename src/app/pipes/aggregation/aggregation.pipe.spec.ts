import { AggregationPipe } from './aggregation.pipe';

describe('AggregationPipe', () => {
  it('create an instance', () => {
    const pipe = new AggregationPipe();
    expect(pipe).toBeTruthy();
  });
});
